# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users
- import: main

transformers:
- kind: Job
  connector: csv
  connector_options:
    #TODO
  property_map:
    - property: recipient_role
      external_name: recipient_role
    - property: year
      external_name: fiscal_year
      import_transform: main.parse_int
    - property: qtr
      external_name: fiscal_qtr
      import_transform: main.parse_int
    - property: recipient_duns
      external_name: recipient_duns_number
      import_transform: main.parse_long
    - property: recipient_name
      external_name: recipient_name
    - property: recipient_zip
      external_name: recipient_zip_code
      import_transform: main.parse_long
    - property: award_type
      external_name: award_type
    - property: amount
      external_name: award_amount
      import_transform: main.parse_float
    - property: award_date
      external_name: award_date
    - property: award_desc
      external_name: award_description
      import_transform: db.Text
    - property: project_name
      external_name: project_name
    - property: project_desc
      external_name: project_descriptio
      import_transform: db.Text
    - property: jobs_list
      external_name: job_creation
      import_transform: db.Text
    - property: jobs_count
      external_name: number_of_jobs
      import_transform: main.parse_float
    - property: funding_agency
      external_name: funding_agency_cd
    - property: pop_lat
      external_name: pop_latitude
      import_transform: main.parse_float
    - property: pop_lon
      external_name: pop_longitude
      import_transform: main.parse_float
    - property: pop_addr
      external_name: pop_st_address
    - property: pop_state
      external_name: pop_state_cd
    - property: pop_city
      external_name: pop_city
    - property: pop_cd
      external_name: pop_cong_dist
